global:
  scrape_interval:     100ms # By default, scrape targets every 15 seconds.

  # Attach these labels to any time series or alerts when communicating with
  # external systems (federation, remote storage, Alertmanager).
  external_labels:
    monitor: 'codelab-monitor'
rule_files:
  - 'putm-rules.yaml'
# A scrape configuration containing exactly one endpoint to scrape:
# Here it's Prometheus itself.
scrape_configs:
  # The job name is added as a label `job=<job_name>` to any timeseries scraped from this config.
  - job_name: 'PUTM_Telemetry'

    # Override the global default and scrape targets from this job every 5 seconds.
    scrape_interval: 10ms

    static_configs:
      - targets: ['localhost:8081']
      
  - job_name: 'Xsense'

    # Override the global default and scrape targets from this job every 5 seconds.
    scrape_interval: 10ms

    static_configs:
      - targets: ['localhost:8082']

remote_write:
 
- url: https://prometheus-prod-01-eu-west-0.grafana.net/api/prom/push
  basic_auth:
    username: 589507
    password: eyJrIjoiNTk5MjYyZjc4MmEzZTJmNjlhOTE2NDY2MGJlMzE4ODg5MTRhOThmZSIsIm4iOiJQVVRNX0tFWSIsImlkIjo3MTczMzN9

  remote_timeout: 30s
  follow_redirects: true
  queue_config:
    capacity: 5000
    max_shards: 400
    min_shards: 1
    max_samples_per_send: 500000
    batch_send_deadline: 5s
    min_backoff: 30ms
    max_backoff: 100ms
    retry_on_http_429: true
  metadata_config:
    max_samples_per_send: 100000
    send: true
    send_interval: 100ms

