#include "Prometheus.hpp"

/*
Data::Bms_Lv *lv = new Data::Bms_Lv();
Data::Apps *apps = new Data::Apps();
Data::Bms_Hv *hv = new Data::Bms_Hv();
Data::AQ_Card *aq = new Data::AQ_Card();
Data::Traction_Control *tc = new Data::Traction_Control();
Data::Temperatures *temps = new Data::Temperatures();
Data::Time *times = new Data::Time();
*/


/*
void Apps::Update_Metrics() 
{
    Pedal_Position.Set();
    Difference    .Set();
     //loggr.Push_Error(&loggr.APPS_Logger,"APPS", apps_string_states[msga->apps_state]);
}

void Bms_Lv::Update_Metrics()
{
    Voltage    .Set((float)   ());
    SoC        .Set((uint8_t) ());
    Temperature.Set((uint8_t) ());
    Current    .Set((uint8_t) ());
    //loggr.Push_Warning(&loggr.BMSLV_Logger,"BMS_LV", bmslv_string_states[msga->bmslv_state]);
    //loggr.Push_Error(&loggr.BMSLV_Logger,"BMS_LV", bmslv_string_states[msga->bmslv_state]);
}

void Bms_Hv::Update_Metrics()
{
    Voltage        .Set((float)   ());
    Current        .Set((float)   ());
    SoC            .Set((float)   ());
    Temperature_max.Set((uint8_t) ());
    Temperature_avg.Set((uint8_t) ());

    //loggr.Push_Warning(&loggr.BMSHV_Logger,"BMS_HV", bmshv_string_states[msga->bmshv_state]);
    //loggr.Push_Error(&loggr.BMSHV_Logger,"BMS_HV", bmshv_string_states[msga->bmshv_state]);
}
void Fuse::Update_Metrics()
{
       
}

void AQ_Card::Update_Metrics()
{
        Susp_front_right    .Set();
        Susp_front_left     .Set();
        brake_pressure_front.Set();
        brake_pressure_rear .Set();
        speed_x             .Set();
        speed_y             .Set();
        speed_z             .Set();
        acc_x               .Set();
        acc_y               .Set();
        acc_z               .Set();
        //loggr.Push_Error(&loggr.AQ_Logger,"Aq_Card", aq_string_states[msgb->aq_state]);
        
}
void Time::Update_Time()
{

    Acc_time.Set();
}
void Traction_Control::Update_Metrics()
{
        vehicle_speed        .Set();
        motor_current        .Set();
        motor_speed          .Set();
        tc_intensity         .Set();
        suspension_rear_left .Set();
        suspension_rear_right.Set();
        wheel_front_left     .Set();
        wheel_front_right    .Set();
        wheel_rear_left      .Set();
        wheel_rear_right     .Set();
        acc_x                .Set();
        acc_y                .Set();
        acc_z                .Set();
        gyro_x               .Set();
        gyro_y               .Set();
        gyro_z               .Set();
        //loggr.Push_Error(&loggr.TC_Logger,"TC", tc_string_states[msgc->tc_state]);
}
void Temperatures::Update_Metrics()
{
    temp_engine.Set   ();
    temp_inverter.Set ();
    temp_water_in.Set ();
    temp_water_out.Set();
    water_p_in.Set    ();
    water_p_out.Set   ();
}
*/
